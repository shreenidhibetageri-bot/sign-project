# sign-project
his project is a real-time Sign Language Recognition system built using Python, OpenCV, and TensorFlow (CNN model). It captures hand gestures through a webcam and predicts the corresponding alphabet letter using a trained deep learning model.
